#include <stdio.h>
int main()
{
    // Step 1: set the variable below
        // type: float
        // identifer: euler
        // value: 2.718
    // Step 2: set the variable below
        // type: double
        // identifer: pie
        // value: 3.1415
    // Step 3: print the string "euler = %" where % equals the value. Of course do this using printf() and the float format specifier
    // Step 4: print the string "pie = %" where % equals the value. Of course do this using printf() and the double format specifier

    // v2: how would you make sure that the two print lines are on separate lines?
    return 0;
}