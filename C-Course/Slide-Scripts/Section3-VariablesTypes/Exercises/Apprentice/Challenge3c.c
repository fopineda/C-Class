#include <stdio.h>
int main()
{

    // Step 1: set the variable below
        // type: char
        // identifer: myChar
        // value: a
    // Step 2: print the string "Character = %" where % equals the value. Of course do this using printf() and the char format specifier

    return 0;
} 