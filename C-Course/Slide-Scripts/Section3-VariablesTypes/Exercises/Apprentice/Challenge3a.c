#include <stdio.h>    
int main()
{ 
    // Step 1: set the variable below
        // type: int
        // identifer: myInt
        // value: 20
    // Step 2: print the string "myInt = %" where % equals the value. Of course do this using printf() and the int format specifier
    
    return 0;
}