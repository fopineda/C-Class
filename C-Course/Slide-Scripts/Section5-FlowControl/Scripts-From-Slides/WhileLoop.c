#include <stdio.h>

int main()
{
    int i = 1;  // I had to initialize this :/
    
    while (i <= 5)
    {
        printf("%d\n", i);
        ++i;   // I had to increment this :/
    }

    return 0;
}